{
  "name": "leet-cli",
  "version": "0.3.1",
  "description": "A terminal workflow for LeetCode company problem lists: browse, solve, test, and sync solved status with your LeetCode account.",
  "type": "module",
  "bin": {
    "leet": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "build:data": "bun run scripts/build-data.ts && bun run scripts/gen-embed.ts",
    "gen:embed": "bun run scripts/gen-embed.ts",
    "test": "bun test",
    "compile": "bun run scripts/gen-embed.ts && bun build src/cli.ts --compile --outfile leet",
    "setup": "bun run scripts/setup.ts"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "license": "MIT"
}
